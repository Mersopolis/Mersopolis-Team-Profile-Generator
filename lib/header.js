const Component = require('./component.js');
const {  } = require('./');

class Header extends Component {
  render() {
    return `<header class="header"><h1>${title}'s Team</h1></header>`;
  }
}

module.exports = Header;
